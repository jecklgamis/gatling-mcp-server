apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gatling-mcp-server.fullname" . }}-test-connection"
  labels:
    {{- include "gatling-mcp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gatling-mcp-server.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
